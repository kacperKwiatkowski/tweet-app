sudo: required

language: java

jdk: oraclejdk17

services:
  - docker

before_install:
  - echo %JAVA_HOME%
  - echo $JAVA_HOME
  - mvn clean package
  - docker build -t kacperkwiatkowski/backend-tweet -f Dockerfile .

script:
  - docker images
